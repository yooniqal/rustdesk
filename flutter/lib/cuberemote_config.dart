// CubeRemote 커스텀 설정 (뷰어 앱 내장 값). 개인용 포크.
class CubeRemoteConfig {
  // 콘솔(가맹점 목록/접속수단) 주소
  static const String consoleUrl = 'https://console.ceomate.co.kr';
  // 뷰어 목록 API 토큰 (콘솔 VIEWER_TOKEN 과 일치해야 함)
  static const String viewerToken = '0dc47642212582d5febfdf933bfa52da';
  // 가맹점 접속 공통 비밀번호(무인 접속용, 콘솔 AGENT_FIXED_PASSWORD 와 동일)
  static const String fixedPassword = 'Cubemns99!';
}
